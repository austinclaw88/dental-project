{
  "name": "@nightshift/recoup-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Recoup Audit web front end — upload a claims CSV, map its columns, see what insurance owes you.",
  "main": "src/main.ts",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/multipart": "^9.0.3",
    "@nightshift/recoup-audit": "*",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
